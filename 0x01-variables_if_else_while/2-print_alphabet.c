#include <stdio.h>
#include <ctype.h>
/**
 * main - a function change to uppercae
 * Return: 0
 */
int main(void)
{
	char ch;

	int putchar(int ch)
        ch = scanf();
	putchar(toupper(ch));
	return (0);
}
